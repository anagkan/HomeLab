# Setting up Traefik

[Traefik Documentation](https://doc.traefik.io/traefik/)

acme.json must have 600 permissions.

```
touch acme.json
chmod 600 acme.json
```

To run container:
```
docker compose up -d
```