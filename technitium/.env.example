# Copy to .env and set this node's Tailscale IP (find it with: tailscale ip -4).
# Technitium's DNS (53) and web console (5380) bind ONLY to this address, so the
# server is reachable over the tailnet and never exposed on the public interface.
TAILSCALE_IP=100.x.y.z
